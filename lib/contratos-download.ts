export const abrirContratoPDF = async (
  _contratoId: string,
  _arquivoNome: string | undefined,
  _token: string,
): Promise<void> => {
  alert('Use o botão Abrir PDF que aparece ao lado — ele agora abre direto do armazenamento.')
}
